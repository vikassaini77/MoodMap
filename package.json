{
  "name": "moodmap-root",
  "version": "1.0.0",
  "description": "Root script to run all MoodMap services concurrently",
  "scripts": {
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && npm run dev",
    "start:ml": "cd backend_ml && uvicorn main:app --reload",
    "dev": "concurrently \"npm run start:frontend\" \"npm run start:backend\" \"npm run start:ml\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
